/** Top-rated drivers notified first on open (broadcast) bookings. */
export const DISPATCH_RATING_WAVE1_SIZE = 10;

/** Remaining matching drivers get push after this delay (ms). */
export const DISPATCH_RATING_WAVE2_DELAY_MS = 3 * 60 * 1000;
